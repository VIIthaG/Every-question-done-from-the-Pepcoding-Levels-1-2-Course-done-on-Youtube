ok so the main stratgy for doing these pattern problems is to simply make 2 variables for spaces ( if needed ) and stars.
Then, simply print them out ( that is print out the variables which should show up as numbers on printing ) once to get an idea of how the pattern will be printed and then finally use them as loop limits when making the for loops

Also i never used any arrays for pattern programs at all
